{
    "version": "1.00",
    "level": "INFO",
    "_comment": "Console (stdout) is always configured. File handlers are opt-in for bare-metal deployments; containers should leave them disabled and let the orchestrator collect stdout.",
    "files": {
        "enabled": false,
        "app": "logs/app.log",
        "security": "logs/security.log",
        "audit": "logs/audit.log",
        "max_bytes": 10485760,
        "backup_count": 5
    }
}
