"""
Reusable test fixtures and object builders.

Every module in this package is imported by ``tests/conftest.py`` so its
fixtures are visible to the whole suite. Splitting them by subject area keeps
each file inside the project's 150-line limit while still giving tests a single
place to look for shared setup.
"""
