"""
The shipped detectors.

The registry imports every module in this package and instantiates each
non-abstract BaseDetector subclass it finds, so adding a detector means adding
a file here and nothing else.
"""
